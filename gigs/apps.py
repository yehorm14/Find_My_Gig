from django.apps import AppConfig

class GigsConfig(AppConfig):
    """
    Configuration for the primary 'gigs' application.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "gigs"